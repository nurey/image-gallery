
<div class="container">
  <ul class="gallery">
    <% @images.each do |image| %>
      <li><%= link_to image_tag(image.picture.thumb.url, alt: image.alt), image_path(image) %></li>
    <% end %>
  </ul>

  <br>

  <%= link_to 'New Image', new_image_path %>
</div>
